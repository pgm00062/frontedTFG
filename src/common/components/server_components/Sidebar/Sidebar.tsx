import SidebarContainer from '../../client_components/SidebarUI/Delivery/components/SidebarContainer'

export default async function SidebarServer() {
  // Aquí podríamos leer cookies y condicionar opciones en el futuro
  return <SidebarContainer />
}